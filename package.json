{
  "private": true,
  "dependencies": {
    "laravel-elixir": "^4.0.0",
    "bootstrap-sass": "^3.0.0"
  },
  "scripts": {
    "postinstall": "npm install --global gulp && gulp"
  },
  "devDependencies": {
    "gulp": "^3.9.1"
  }
}
